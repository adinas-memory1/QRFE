<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      LAN POS: agent local print (http://{lan-ip}:9247) and FiscalNet must use cleartext.
      Domains below cover API host + common hotspot/LAN agent IPs; base-config allows
      other RFC1918 agent addresses advertised via heartbeat (IP changes with Wi-Fi).
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.43.142</domain>
        <domain includeSubdomains="false">192.168.43.237</domain>
    </domain-config>
</network-security-config>
